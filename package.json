{
    "name": "auto-sub",
    "author": "Doan Qang Huy",
    "license": "MIT",
    "engines": {
        "node": ">=22"
    },
    "version": "1.0.0",
    "description": "Chrome Extension for speech recognition and translation",
    "scripts": {
        "build": "tsc && node build.js",
        "watch": "tsc --watch",
        "clean": "rm -rf dist",
        "format": "prettier --write \"src/**/*.ts\""
    },
    "devDependencies": {
        "@types/chrome": "^0.0.268",
        "esbuild": "^0.19.0",
        "typescript": "^5.3.3"
    },
    "dependencies": {
        "prettier": "^3.6.2"
    }
}